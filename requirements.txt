# Synthetic Health Cost Growth Target Analytics
# Pinned to minor versions for reproducibility; loosen if you need newer.
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.7
pytest>=7.4
streamlit>=1.30      # interactive dashboard (src/dashboard.py)
# Optional:
# duckdb>=0.10
